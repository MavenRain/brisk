let choose n = match n with | 0 -> { b = 7, a = 1 } | other -> { a = 2, b = 8 }
let read r = (r : { a : int, b : int }).b
let first = print_int (read (choose 0))
let main = print_int (read (choose 1))
