let read v = match (v : < box : < a : int, b : int >, other : int >) with | < box < a n > > -> n | remaining -> 7
let main = let _ = print_int (read (< box < a 3 > >)) in let _ = print_int (read (< box < b 4 > >)) in print_int (read (< other 5 >))
