; test/dune:  parse.exe of M0-PLAN.md:248, main.exe of M0-PLAN.md:249 and
; vm.exe of M0-PLAN.md:250.
; One stanza declares all three, because they read the same libraries
; (D-B-20, D-D-34).
; The libraries brisk_core, brisk_surface and brisk_vm are unwrapped, so
; Error, Ast, Parser, Print, Infer, Lower, Instr, Assemble and Exec name
; themselves (D-A-11).
; -warn-error +a is the "warnings as errors" of M0-PLAN.md:245 (D-A-10).

(executables
 (names parse main vm)
 (libraries brisk_core brisk_surface brisk_vm)
 (flags (:standard -warn-error +a)))
