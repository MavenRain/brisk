; test/dune:  parse.exe, the PARSE gate leg of M0-PLAN.md:248.
; The executable reads brisk_core and brisk_surface, which are unwrapped,
; so Error, Ast, Parser and Print name themselves (D-A-11).
; -warn-error +a is the "warnings as errors" of M0-PLAN.md:245 (D-A-10).

(executable
 (name parse)
 (libraries brisk_core brisk_surface)
 (flags (:standard -warn-error +a)))
